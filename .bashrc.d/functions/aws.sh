function osim_aws_get_kubeconfigs() {
  # aws eks list-clusters
  # aws eks update-kubeconfig --region us-east-1 --name "${cluster_name}" --kubeconfig ~/.kube/"${cluster_name}".yaml
  echo "Not Implemented Yet.."
}
